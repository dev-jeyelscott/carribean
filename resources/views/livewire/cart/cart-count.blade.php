<span
    class="inline-flex min-h-5 min-w-5 items-center justify-center
        rounded-full bg-brand-coral px-1.5 text-[0.65rem]
        font-bold leading-none text-white"
    aria-label="{{ $count }} item{{ $count === 1 ? '' : 's' }} in cart">
    {{ $count }}
</span>